# Registru Digital Bibliotecă — dependențe Python
# Python 3.8 (necesar pentru Windows 7 — vezi BUILD_WINDOWS7.md) · v1.7.0
#
# ⚠ Windows 7: folosiți Python 3.8 pe 32 de biți. Python 3.9+ NU pornește pe Win7,
#   iar PyQt6/Qt6 cere Windows 10+. Această versiune folosește PyQt5/Qt5 (suportă Win7).
#
# Instalare completă (dezvoltare + teste + build):
#   install_dependencies.bat          # doar runtime, pentru bibliotecar/dev rapid
#   venv\Scripts\pip install -r requirements.txt   # tot (inclusiv pytest, PyInstaller)
#
# Rulare aplicație:
#   run.bat
#
# Teste:
#   set PYTHONPATH=app
#   venv\Scripts\python.exe -m pytest tests\ -v
#
# Build distribuție:
#   build.bat              # portabil + ghid PDF
#   build_installer.bat    # portabil + instalator Inno Setup

# --- Runtime (aplicație desktop) ---

PyQt5==5.15.10          # Qt5 — ultima serie Qt care suportă Windows 7
SQLAlchemy>=2.0,<2.1
openpyxl>=3.1,<3.2      # export / import Excel
python-docx>=1.1,<1.2   # export Word (.docx)
reportlab>=4.0,<4.3     # export PDF + ghid bibliotecar (fonturi în app/resources/fonts/)

# --- Dezvoltare și teste ---

pytest>=8.0,<9
pytest-qt>=4.4          # smoke tests PyQt (MainWindow, tabel, onboarding)

# --- Build distribuție Windows ---

pyinstaller>=6.0,<7      # build.bat / build_installer.bat → dist/RegistruDigital/
